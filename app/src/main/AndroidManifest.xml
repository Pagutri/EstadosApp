<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.estados">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GasIdealActivity" />
        <activity android:name=".DietericiActivity" />
        <activity android:name=".VanDerWaalsActivity" />
        <activity android:name=".MargulesActivity" />
        <activity android:name=".PengRobinsonActivity" />
        <activity android:name=".RedlichKwongActivity" />
        <activity android:name=".SoaveActivity" />
        <activity android:name=".VanLaarActivity" />
        <activity android:name=".WilsonActivity" />
        <activity android:name=".PlotVanDerWaalsActivity" />
    </application>

</manifest>